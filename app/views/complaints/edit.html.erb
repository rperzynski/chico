<div class="alert alert-info">
<h2>Edit Your Complaint</h2>
</div>

<%= render 'form' %>

<ul class="nav nav-tabs nav-stacked">
<li><%= link_to 'Back', ward_complaints_path(@ward) %></li>
<li><% if :current_user %>
               <%= link_to session[:user_id] %>
								<% else %>
							 <%= link_to "Connect With Twitter", "/auth/twitter" %>
                <% end %></li>
</ul>
